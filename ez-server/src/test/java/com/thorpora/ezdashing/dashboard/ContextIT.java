package com.thorpora.ezdashing.dashboard;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ContextIT {

//  @Bean
//  public DashboardConfiguration dashboardConfiguration() {
//    URL url = this.getClass().getClassLoader().getResource(FILENAME);
//    String errorMsg = format("Unable to find the dashboard config resource '%s'", FILENAME);
//    String configFile = requireNonNull(url, errorMsg).getFile();
//    return new DashboardConfiguration(new File(configFile));
//  }

}
