import { Anchor, Props, Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import AudioPlayerWidget from "./audio-player-widget";
import Widget from "component/widget/base/widget";
import { array, object, text, withKnobs } from "@storybook/addon-knobs";
import { linkTo } from '@storybook/addon-links'

<Meta 
  title="Components|Widget/AudioPlayer" 
  parameters={{
    githubLink: "/blob/master/ez-client/src/main/js/component/widget/audio/audio-player-widget.jsx"
  }}
/>

                 
<Anchor storyId="components-widget-audioplayer--default-story" />


# AudioPlayer

This is a simple audio player, build for fun. It offers some options and a playlist. This component is working 
but is still in development. The idea is to have, in the future, a music server where all dashboard users 
could interact with a common playlist.

## Props
 
As all widgets, you can override <a onClick={linkTo('components-widget-widget--default-story')}>common widget 
properties</a>.

<Props of={AudioPlayerWidget} />

## Usage

### Default

<Preview>
  <Story
    name="default"
    parameters={{
      width: "500px",
      height: "200px"
    }}
  >
    <AudioPlayerWidget/>
  </Story>
</Preview>

<Story 
  name="knobs"
  decorators={[withKnobs]}
  parameters={{
    width: "500px",
    height: "200px",
    docs: {
      disable: true
    }
  }}
>
  <AudioPlayerWidget
    id={text("id", "sampleId")}
    loader={text("loader", "<p>Widget loading...</p>")}
    key={text("key", "sampleId")}
    title={text("title", "Knobs AudioPlayer")}
  />
</Story>
